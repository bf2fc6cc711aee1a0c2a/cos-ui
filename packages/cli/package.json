{
  "name": "@kas-connectors/cli",
  "version": "0.0.2",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mono/mono.git",
    "directory": "packages/cli"
  },
  "scripts": {
    "build": "tsc --project tsconfig.cli.json",
    "start": "yarn build && dist/cli.js --token $(ocm token) --server http://localhost:8000",
    "test": "tsdx test",
    "lint": "tsdx lint",
    "prepublish": "npm run build"
  },
  "bin": "dist/cli.js",
  "engines": {
    "node": ">=10"
  },
  "files": [
    "README.md",
    "dist"
  ],
  "peerDependencies": {
    "react": "^16.8.0"
  },
  "dependencies": {
    "@kas-connectors/machines": "^0.0.1",
    "@kas-connectors/api": "^0.0.1",
    "@xstate/react": "^1.3.1",
    "ink": "^3.0.8",
    "ink-select-input": "^4.2.0",
    "ink-spinner": "^4.0.1",
    "meow": "^9.0.0",
    "tslib": "^2.0.0",
    "xstate": "^4.17.1"
  },
  "publishConfig": {
    "access": "public"
  }
}
